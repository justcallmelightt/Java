# 1번 문제 - 랜덤 주사위 시뮬레이터

- 원문: http://yubs.online/java/(1%ED%95%99%EA%B8%B0)_%EA%B8%B0%EC%B4%88/01_%EC%9E%90%EB%B0%94-%EC%96%B8%EC%96%B4%EC%9D%98-%EC%9D%B4%ED%95%B4-(%EB%AC%B8%EC%A0%9C%ED%92%80%EC%9D%B4)/98_%EB%AC%B8%EC%A0%9C%ED%92%80%EC%9D%B4-6-(%EC%88%98%ED%96%89-%EC%A4%80%EB%B9%84-%EC%A0%84-%EC%B6%94%EA%B0%80-%EB%AC%B8%EC%A0%9C%ED%92%80%EC%9D%B4).html
- 용도: yubs.online 수업 페이지의 코드 블록을 원문 순서대로 보존한 학습 자료입니다.
- 실행 가능한 예제는 같은 단원의 Java 파일을 우선 참고하세요.

## 코드 블록 1

```text
메뉴를 선택해 주사위를 굴리거나 프로그램을 종료하는 CLI 프로그램을 작성하시오.

메뉴 :
1. 주사위 굴리기
2. 종료

조건1. 주사위를 굴릴 때마다 1~6 사이의 정수를 랜덤으로 출력한다.
조건2. 종료를 선택하면 지금까지 총 몇 번 굴렸는지 출력하고 프로그램을 마친다.

실행 예시 :
===== 주사위 시뮬레이터 =====
1. 주사위 굴리기
2. 종료
메뉴 선택: 1
결과: 4

메뉴 선택: 1
결과: 1

메뉴 선택: 2
총 2번 굴렸습니다.
프로그램을 종료합니다.
```

## 코드 블록 2

```text
import java.util.Scanner;

public class DiceSimulator {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int count = 0;

        while (true) {
            System.out.println("===== 주사위 시뮬레이터 =====");
            System.out.println("1. 주사위 굴리기");
            System.out.println("2. 종료");
            System.out.print("메뉴 선택: ");
            int menu = sc.nextInt();

            if (menu == 2) {
                System.out.println("총 " + count + "번 굴렸습니다.");
                System.out.println("프로그램을 종료합니다.");
                break;
            } else if (menu == 1) {
                int result = (int)(Math.random() * 6) + 1;
                System.out.println("결과: " + result);
                count++;
            } else {
                System.out.println("잘못된 메뉴입니다.");
            }
            System.out.println();
        }
        sc.close();
    }
}
```

## 코드 블록 3

```text
사용자가 메뉴로 가위/바위/보를 선택하면 컴퓨터가 랜덤으로 하나를 선택하여 승부를 겨루는 프로그램을 작성하시오.

메뉴 :
1. 가위
2. 바위
3. 보
4. 종료

조건1. 컴퓨터의 선택은 1~3 사이의 정수를 랜덤으로 뽑아 결정한다.
조건2. 승, 패, 무 결과를 매 판마다 출력한다.
조건3. 승/패/무 횟수를 각각 누적하여 배열에 저장한다.
조건4. 종료를 선택하면 누적된 전적(승/패/무)을 출력한다.

실행 예시 :
===== 가위바위보 =====
1. 가위
2. 바위
3. 보
4. 종료
메뉴 선택: 2
나: 바위 / 컴퓨터: 가위
승리

메뉴 선택: 1
나: 가위 / 컴퓨터: 보
승리

메뉴 선택: 4
전적 - 승: 2 패: 0 무: 0
프로그램을 종료합니다.
```

## 코드 블록 4

```text
import java.util.Scanner;

public class RockPaperScissors {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int[] record = new int[3]; // 0: 승, 1: 패, 2: 무
        String[] names = {"가위", "바위", "보"};

        while (true) {
            System.out.println("===== 가위바위보 =====");
            System.out.println("1. 가위");
            System.out.println("2. 바위");
            System.out.println("3. 보");
            System.out.println("4. 종료");
            System.out.print("메뉴 선택: ");
            int user = sc.nextInt();

            if (user == 4) {
                System.out.println("전적 - 승: " + record[0] + " 패: " + record[1] + " 무: " + record[2]);
                System.out.println("프로그램을 종료합니다.");
                break;
            }
            if (user < 1 || user > 3) {
                System.out.println("잘못된 메뉴입니다.");
                System.out.println();
                continue;
            }

            int com = (int)(Math.random() * 3) + 1;
            System.out.println("나: " + names[user - 1] + " / 컴퓨터: " + names[com - 1]);

            if (user == com) {
                System.out.println("무승부");
                record[2]++;
            } else if ((user == 1 && com == 3) || (user == 2 && com == 1) || (user == 3 && com == 2)) {
                System.out.println("승리");
                record[0]++;
            } else {
                System.out.println("패배");
                record[1]++;
            }
            System.out.println();
        }
        sc.close();
    }
}
```

## 코드 블록 5

```text
주사위를 굴리고 그 결과를 기록하는 프로그램을 작성하시오.

메뉴 :
1. 굴리기
2. 기록 보기
3. 최댓값 / 최솟값 보기
4. 종료

조건1. 굴린 결과 하나하나를 DiceRecord 객체로 만들어 저장한다.
조건2. 주사위를 굴리는 기능과 기록 배열은 Dice 클래스 안에서 관리한다.
조건3. 한 번도 굴리지 않은 상태에서 2번 또는 3번 메뉴를 선택하면 "기록이 없습니다."를 출력한다.
조건4. 기록 배열의 크기는 100으로 고정하며, 100번 이상 굴리지 않는다고 가정한다.
조건5. 지금까지 굴린 횟수를 내부적으로 관리한다.

DiceRecord 클래스 명세 :
  - 필드 : int round (몇 번째 굴렸는지), int value (굴린 결과값) — private으로 선언
  - 생성자 : round와 value를 매개변수로 받는다.
  - 게터 : getRound(), getValue()
  - 세터 : setRound(int round), setValue(int value)

Dice 클래스 명세 :
  - int roll()           : 1~6 사이의 정수를 랜덤으로 반환하고 DiceRecord를 만들어 내부에 기록한다.
  - boolean hasHistory() : 한 번이라도 굴린 적이 있으면 true를 반환한다.
  - void printHistory()  : 기록된 결과를 "1번째: 4, 2번째: 2" 형식으로 출력한다.
  - int getMax()         : 기록 중 최댓값을 반환한다.
  - int getMin()         : 기록 중 최솟값을 반환한다.

실행 예시 :
===== 주사위 게임 =====
1. 굴리기
2. 기록 보기
3. 최댓값/최솟값 보기
4. 종료
메뉴 선택: 1
결과: 4

메뉴 선택: 1
결과: 2

메뉴 선택: 2
1번째: 4
2번째: 2

메뉴 선택: 3
최댓값: 4 / 최솟값: 2

메뉴 선택: 4
프로그램을 종료합니다.
```

## 코드 블록 6

```text
import java.util.Scanner;

class DiceRecord {
    private int round;
    private int value;

    DiceRecord(int round, int value) {
        this.round = round;
        this.value = value;
    }

    int getRound() { return round; }
    int getValue() { return value; }
    void setRound(int round) { this.round = round; }
    void setValue(int value) { this.value = value; }
}

class Dice {
    private DiceRecord[] history = new DiceRecord[100];
    private int rollCount = 0;

    int roll() {
        int result = (int)(Math.random() * 6) + 1;
        history[rollCount] = new DiceRecord(rollCount + 1, result);
        rollCount++;
        return result;
    }

    boolean hasHistory() {
        return rollCount > 0;
    }

    void printHistory() {
        for (int i = 0; i < rollCount; i++) {
            System.out.println(history[i].getRound() + "번째: " + history[i].getValue());
        }
    }

    int getMax() {
        int max = history[0].getValue();
        for (int i = 1; i < rollCount; i++) {
            if (history[i].getValue() > max) {
                max = history[i].getValue();
            }
        }
        return max;
    }

    int getMin() {
        int min = history[0].getValue();
        for (int i = 1; i < rollCount; i++) {
            if (history[i].getValue() < min) {
                min = history[i].getValue();
            }
        }
        return min;
    }
}

public class DiceGame {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        Dice dice = new Dice();

        while (true) {
            System.out.println("===== 주사위 게임 =====");
            System.out.println("1. 굴리기");
            System.out.println("2. 기록 보기");
            System.out.println("3. 최댓값/최솟값 보기");
            System.out.println("4. 종료");
            System.out.print("메뉴 선택: ");
            int menu = sc.nextInt();

            if (menu == 4) {
                System.out.println("프로그램을 종료합니다.");
                break;
            }

            switch (menu) {
                case 1:
                    System.out.println("결과: " + dice.roll());
                    break;
                case 2:
                    if (!dice.hasHistory()) {
                        System.out.println("기록이 없습니다.");
                    } else {
                        dice.printHistory();
                    }
                    break;
                case 3:
                    if (!dice.hasHistory()) {
                        System.out.println("기록이 없습니다.");
                    } else {
                        System.out.println("최댓값: " + dice.getMax() + " / 최솟값: " + dice.getMin());
                    }
                    break;
                default:
                    System.out.println("잘못된 메뉴입니다.");
            }
            System.out.println();
        }
        sc.close();
    }
}
```

## 코드 블록 7

```text
확률에 따라 등급이 결정되는 뽑기(가챠) 프로그램을 작성하시오.

메뉴 :
1. 뽑기
2. 결과 통계 보기
3. 종료

조건1. 등급은 일반(70%), 희귀(25%), 전설(5%) 세 가지이다.
조건2. 뽑기 기능과 등급별 누적 횟수는 GachaMachine 클래스 안에서 관리한다.
조건3. 0~99 사이의 정수를 뽑아 0~69는 일반, 70~94는 희귀, 95~99는 전설로 판정한다.

GachaMachine 클래스 명세 :
  - String pull()     : 뽑기를 실행하고 해당 등급 이름을 반환한다.
  - void printStats() : 총 뽑기 횟수와 등급별 누적 횟수를 출력한다.

실행 예시 :
===== 가챠 시뮬레이터 =====
1. 뽑기
2. 결과 통계 보기
3. 종료
메뉴 선택: 1
결과: 일반

메뉴 선택: 1
결과: 희귀

메뉴 선택: 2
총 뽑기 횟수: 2회
일반: 1회
희귀: 1회
전설: 0회

메뉴 선택: 3
프로그램을 종료합니다.
```

## 코드 블록 8

```text
import java.util.Scanner;

class GachaMachine {
    private int[] counts = new int[3]; // 0: 일반, 1: 희귀, 2: 전설
    private String[] names = {"일반", "희귀", "전설"};

    String pull() {
        int rand = (int)(Math.random() * 100); // 0~99
        int grade;
        if (rand < 70) {
            grade = 0;
        } else if (rand < 95) {
            grade = 1;
        } else {
            grade = 2;
        }
        counts[grade]++;
        return names[grade];
    }

    void printStats() {
        int total = counts[0] + counts[1] + counts[2];
        System.out.println("총 뽑기 횟수: " + total + "회");
        for (int i = 0; i < counts.length; i++) {
            System.out.println(names[i] + ": " + counts[i] + "회");
        }
    }
}

public class GachaSimulator {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        GachaMachine machine = new GachaMachine();

        while (true) {
            System.out.println("===== 가챠 시뮬레이터 =====");
            System.out.println("1. 뽑기");
            System.out.println("2. 결과 통계 보기");
            System.out.println("3. 종료");
            System.out.print("메뉴 선택: ");
            int menu = sc.nextInt();

            if (menu == 3) {
                System.out.println("프로그램을 종료합니다.");
                break;
            }

            switch (menu) {
                case 1:
                    String result = machine.pull();
                    System.out.println("결과: " + result);
                    break;
                case 2:
                    machine.printStats();
                    break;
                default:
                    System.out.println("잘못된 메뉴입니다.");
            }
            System.out.println();
        }
        sc.close();
    }
}
```
