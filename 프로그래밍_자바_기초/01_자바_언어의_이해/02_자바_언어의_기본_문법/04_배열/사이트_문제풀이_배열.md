# 문제 풀이 - 배열

- 원문: http://yubs.online/java/(1%ED%95%99%EA%B8%B0)_%EA%B8%B0%EC%B4%88/01_%EC%9E%90%EB%B0%94-%EC%96%B8%EC%96%B4%EC%9D%98-%EC%9D%B4%ED%95%B4-(%EB%AC%B8%EC%A0%9C%ED%92%80%EC%9D%B4)/98_%EB%AC%B8%EC%A0%9C%ED%92%80%EC%9D%B4-5-(%EC%A0%9C%EC%96%B4%EB%AC%B8,-%EB%B0%98%EB%B3%B5%EB%AC%B8,-%EB%B0%B0%EC%97%B4-%EC%9C%B5%ED%95%A9).html
- 용도: yubs.online 수업 페이지의 코드 블록을 원문 순서대로 보존한 학습 자료입니다.
- 실행 가능한 예제는 같은 단원의 Java 파일을 우선 참고하세요.

## 코드 블록 1

```text
정수 N개를 입력받아 배열에 저장하고, 역순으로 출력하는 프로그램을 작성하시오.

입력 예시 :
N 입력 : 5
1번째 정수 : 3
2번째 정수 : 7
3번째 정수 : 1
4번째 정수 : 9
5번째 정수 : 4

출력 예시 : 4 9 1 7 3
```

## 코드 블록 2

```text
import java.util.Scanner;

public class ArrayReverse {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        for (int i = nums.length - 1; i >= 0; i--) {
            System.out.print(nums[i] + " ");
        }
        System.out.println();
        sc.close();
    }
}
```

## 코드 블록 3

```text
정수 N개를 입력받아 배열에 저장하고, 찾을 값을 입력받아 해당 값이 몇 번 인덱스에 있는지 출력하는 프로그램을 작성하시오.

조건1. 값이 없으면 "찾을 수 없습니다."를 출력한다.
조건2. 같은 값이 여러 개면 모든 인덱스를 출력한다.

입력 예시 :
N 입력 : 8
1번째 정수 : 3
2번째 정수 : 1
3번째 정수 : 4
4번째 정수 : 1
...
8번째 정수 : 6
찾을 값 : 1

출력 예시 :
인덱스 1
인덱스 3
```

## 코드 블록 4

```text
import java.util.Scanner;

public class ArraySearch {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        System.out.print("찾을 값 : ");
        int target = sc.nextInt();
        boolean found = false;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == target) {
                System.out.println("인덱스 " + i);
                found = true;
            }
        }
        if (!found) {
            System.out.println("찾을 수 없습니다.");
        }
        sc.close();
    }
}
```

## 코드 블록 5

```text
정수 N개를 입력받아 배열에 저장하고, 새 배열에 복사한 뒤 두 배열을 출력하는 프로그램을 작성하시오.

조건1. = 대입 연산자로 복사하지 않는다.
조건2. 복사 후 원본 배열의 첫 번째 값을 999로 변경한다.
조건3. 원본과 복사본을 각각 출력하여 서로 독립적임을 확인한다.

힌트 :
- = 으로 배열을 복사하면 주소값만 복사되어 원본과 복사본이 같은 배열을 가리키게 된다.
- 값을 독립적으로 복사하려면 반복문으로 요소를 하나씩 옮겨야 한다.

입력 예시 :
N 입력 : 4
1번째 정수 : 10
2번째 정수 : 20
3번째 정수 : 30
4번째 정수 : 40

출력 예시 :
원본   : 999 20 30 40
복사본 : 10 20 30 40
```

## 코드 블록 6

```text
import java.util.Scanner;

public class ArrayCopy {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] original = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            original[i] = sc.nextInt();
        }
        int[] copy = new int[n];
        for (int i = 0; i < n; i++) {
            copy[i] = original[i];
        }
        original[0] = 999;
        System.out.print("원본   : ");
        for (int i = 0; i < original.length; i++) {
            System.out.print(original[i] + " ");
        }
        System.out.println();
        System.out.print("복사본 : ");
        for (int i = 0; i < copy.length; i++) {
            System.out.print(copy[i] + " ");
        }
        System.out.println();
        sc.close();
    }
}
```

## 코드 블록 7

```text
첫 번째 배열의 크기 N과 두 번째 배열의 크기 M을 각각 입력받고, 두 배열을 하나로 합친 새로운 배열을 만들어 출력하는 프로그램을 작성하시오.

조건1. 합쳐진 새 배열의 크기는 N + M이어야 한다.
조건2. 첫 번째 배열의 요소가 먼저 들어가고, 그 뒤에 두 번째 배열의 요소가 이어져야 한다.

힌트 :
- 크기가 N + M인 새 배열을 선언한다.
- 첫 번째 배열을 0번 인덱스부터 복사하고, 두 번째 배열은 N번 인덱스부터 이어서 복사한다.

입력 예시 :
첫 번째 배열 크기(N) : 3
1번째 정수 : 1
2번째 정수 : 2
3번째 정수 : 3
두 번째 배열 크기(M) : 2
1번째 정수 : 4
2번째 정수 : 5

출력 예시 :
병합된 배열 : 1 2 3 4 5
```

## 코드 블록 8

```text
import java.util.Scanner;

public class ArrayMerge {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("첫 번째 배열 크기(N) : ");
        int n = sc.nextInt();
        int[] arrA = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            arrA[i] = sc.nextInt();
        }
        System.out.print("두 번째 배열 크기(M) : ");
        int m = sc.nextInt();
        int[] arrB = new int[m];
        for (int i = 0; i < m; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            arrB[i] = sc.nextInt();
        }
        int[] merged = new int[n + m];
        for (int i = 0; i < n; i++) {
            merged[i] = arrA[i];
        }
        for (int i = 0; i < m; i++) {
            merged[n + i] = arrB[i];
        }
        System.out.print("병합된 배열 : ");
        for (int i = 0; i < merged.length; i++) {
            System.out.print(merged[i] + " ");
        }
        System.out.println();
        sc.close();
    }
}
```

## 코드 블록 9

```text
정수 N개를 입력받아 배열에 저장하고, 짝수만 골라 출력하는 프로그램을 작성하시오.

조건1. 짝수가 없으면 "짝수 없음"을 출력한다.

입력 예시 :
N 입력 : 6
1번째 정수 : 3
2번째 정수 : 7
3번째 정수 : 2
4번째 정수 : 8
5번째 정수 : 5
6번째 정수 : 4

출력 예시 : 2 8 4
```

## 코드 블록 10

```text
import java.util.Scanner;

public class EvenFromArray {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        boolean found = false;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] % 2 == 0) {
                System.out.print(nums[i] + " ");
                found = true;
            }
        }
        if (!found) {
            System.out.println("짝수 없음");
        } else {
            System.out.println();
        }
        sc.close();
    }
}
```

## 코드 블록 11

```text
정수 N개를 입력받아 배열에 저장하고, 양수, 음수, 0의 개수를 각각 출력하는 프로그램을 작성하시오.

입력 예시 :
N 입력 : 7
1번째 정수 : 3
2번째 정수 : -1
3번째 정수 : 0
...
7번째 정수 : 2

출력 예시 :
양수 : 3개
음수 : 2개
0   : 2개
```

## 코드 블록 12

```text
import java.util.Scanner;

public class CountSign {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        int pos = 0, neg = 0, zero = 0;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] > 0) {
                pos++;
            } else if (nums[i] < 0) {
                neg++;
            } else {
                zero++;
            }
        }
        System.out.println("양수 : " + pos + "개");
        System.out.println("음수 : " + neg + "개");
        System.out.println("0   : " + zero + "개");
        sc.close();
    }
}
```

## 코드 블록 13

```text
학생 N명의 점수를 입력받아 배열에 저장하고, 합계, 평균, 최고점, 최저점을 출력하는 프로그램을 작성하시오.

조건1. 평균은 소수점을 포함하여 출력한다.

입력 예시 :
학생 수 : 5
1번 학생 점수 : 85
2번 학생 점수 : 92
3번 학생 점수 : 61
4번 학생 점수 : 73
5번 학생 점수 : 55

출력 예시 :
합계   : 366
평균   : 73.2
최고점 : 92
최저점 : 55
```

## 코드 블록 14

```text
import java.util.Scanner;

public class ScoreProcess {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("학생 수 : ");
        int n = sc.nextInt();
        int[] scores = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번 학생 점수 : ");
            scores[i] = sc.nextInt();
        }
        int sum = 0;
        int max = scores[0];
        int min = scores[0];
        for (int i = 0; i < scores.length; i++) {
            sum += scores[i];
            if (scores[i] > max) {
                max = scores[i];
            }
            if (scores[i] < min) {
                min = scores[i];
            }
        }
        System.out.println("합계   : " + sum);
        System.out.println("평균   : " + (double) sum / n);
        System.out.println("최고점 : " + max);
        System.out.println("최저점 : " + min);
        sc.close();
    }
}
```

## 코드 블록 15

```text
학생 N명의 점수를 입력받아 배열에 저장하고, 각 학생의 점수와 학점을 출력하는 프로그램을 작성하시오.

조건1. 90점 이상 : A, 80점 이상 : B, 70점 이상 : C, 60점 이상 : D, 60점 미만 : F

입력 예시 :
학생 수 : 5
1번 학생 점수 : 85
2번 학생 점수 : 92
3번 학생 점수 : 61
4번 학생 점수 : 73
5번 학생 점수 : 55

출력 예시 :
1번 학생 : 85점 → B
2번 학생 : 92점 → A
3번 학생 : 61점 → D
4번 학생 : 73점 → C
5번 학생 : 55점 → F
```

## 코드 블록 16

```text
import java.util.Scanner;

public class ScoreGrade {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("학생 수 : ");
        int n = sc.nextInt();
        int[] scores = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번 학생 점수 : ");
            scores[i] = sc.nextInt();
        }
        for (int i = 0; i < scores.length; i++) {
            String grade;
            if (scores[i] >= 90) {
                grade = "A";
            } else if (scores[i] >= 80) {
                grade = "B";
            } else if (scores[i] >= 70) {
                grade = "C";
            } else if (scores[i] >= 60) {
                grade = "D";
            } else {
                grade = "F";
            }
            System.out.println((i + 1) + "번 학생 : " + scores[i] + "점 → " + grade);
        }
        sc.close();
    }
}
```

## 코드 블록 17

```text
선수 N명의 기록(초)을 입력받아 배열에 저장하고, 기록이 가장 빠른 선수의 번호와 기록을 출력하는 프로그램을 작성하시오.

조건1. 선수 번호는 1번부터 시작한다.

입력 예시 :
선수 수 : 4
1번 선수 기록(초) : 312
2번 선수 기록(초) : 287
3번 선수 기록(초) : 305
4번 선수 기록(초) : 299

출력 예시 : 1등 : 2번 선수 (287초)
```

## 코드 블록 18

```text
import java.util.Scanner;

public class RaceRank {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("선수 수 : ");
        int n = sc.nextInt();
        int[] times = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번 선수 기록(초) : ");
            times[i] = sc.nextInt();
        }
        int minIdx = 0;
        for (int i = 1; i < times.length; i++) {
            if (times[i] < times[minIdx]) {
                minIdx = i;
            }
        }
        System.out.println("1등 : " + (minIdx + 1) + "번 선수 (" + times[minIdx] + "초)");
        sc.close();
    }
}
```

## 코드 블록 19

```text
정수 N개를 입력받아 배열에 저장하고, 평균 이상인 값만 출력하는 프로그램을 작성하시오.

입력 예시 :
N 입력 : 6
1번째 정수 : 4
2번째 정수 : 7
3번째 정수 : 2
4번째 정수 : 9
5번째 정수 : 5
6번째 정수 : 8

출력 예시 :
평균 : 5.833333333333333
평균 이상 : 7 9 8
```

## 코드 블록 20

```text
import java.util.Scanner;

public class AboveAverage {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        int sum = 0;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
        }
        double avg = (double) sum / n;
        System.out.println("평균 : " + avg);
        System.out.print("평균 이상 : ");
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] >= avg) {
                System.out.print(nums[i] + " ");
            }
        }
        System.out.println();
        sc.close();
    }
}
```

## 코드 블록 21

```text
상품 N개의 재고 수량을 입력받아 배열에 저장하고, 아래 조건에 따라 출력하는 프로그램을 작성하시오.

조건1. 재고가 0인 상품은 "품절"을 출력한다.
조건2. 재고가 1 이상 5 이하인 상품은 "재고 부족"을 출력한다.
조건3. 재고가 6 이상인 상품은 재고 수량을 출력한다.
조건4. 전체 재고 합계를 마지막에 출력한다.

입력 예시 :
상품 수 : 5
1번 상품 재고 : 0
2번 상품 재고 : 3
3번 상품 재고 : 12
4번 상품 재고 : 5
5번 상품 재고 : 8

출력 예시 :
1번 상품 : 품절
2번 상품 : 재고 부족 (3개)
3번 상품 : 12개
4번 상품 : 재고 부족 (5개)
5번 상품 : 8개
전체 재고 : 28개
```

## 코드 블록 22

```text
import java.util.Scanner;

public class StockManager {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("상품 수 : ");
        int n = sc.nextInt();
        int[] stock = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번 상품 재고 : ");
            stock[i] = sc.nextInt();
        }
        int total = 0;
        for (int i = 0; i < stock.length; i++) {
            total += stock[i];
            if (stock[i] == 0) {
                System.out.println((i + 1) + "번 상품 : 품절");
            } else if (stock[i] <= 5) {
                System.out.println((i + 1) + "번 상품 : 재고 부족 (" + stock[i] + "개)");
            } else {
                System.out.println((i + 1) + "번 상품 : " + stock[i] + "개");
            }
        }
        System.out.println("전체 재고 : " + total + "개");
        sc.close();
    }
}
```

## 코드 블록 23

```text
정수 N개를 입력받아 배열에 저장하고, 중복을 제거하여 처음 등장한 순서대로 출력하는 프로그램을 작성하시오.

힌트 :
- 중복 없는 값을 담을 추가 배열(result)을 크기 N으로 선언한다.
- 원본 배열을 앞에서부터 순서대로 꺼내면서, result 배열의 0번부터 count-1번까지 하나씩 비교하여 같은 값이 있는지 확인한다.
- 아직 없는 값이면 result에 추가하고, 실제로 추가된 개수(count)를 1 증가시킨다.
- 출력은 result 배열 전체가 아니라 count까지만 한다.

입력 예시 :
N 입력 : 7
1번째 정수 : 3
2번째 정수 : 1
3번째 정수 : 4
...
7번째 정수 : 2

출력 예시 : 3 1 4 5 2
```

## 코드 블록 24

```text
import java.util.Scanner;

public class ArrayUnique {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        int[] result = new int[n];
        int count = 0;
        for (int i = 0; i < n; i++) {
            boolean isDuplicate = false;
            for (int j = 0; j < count; j++) {
                if (nums[i] == result[j]) {
                    isDuplicate = true;
                    break;
                }
            }
            if (!isDuplicate) {
                result[count++] = nums[i];
            }
        }
        System.out.print("결과 : ");
        for (int i = 0; i < count; i++) {
            System.out.print(result[i] + " ");
        }
        System.out.println();
        sc.close();
    }
}
```

## 코드 블록 25

```text
정수 N개를 입력받아 배열에 저장하고, 앞에서부터 읽을 때와 뒤에서부터 읽을 때가 같은지 판별하는 프로그램을 작성하시오.

조건1. 대칭이면 "대칭 배열입니다.", 아니면 "대칭 배열이 아닙니다."를 출력한다.

힌트 :
- 배열의 양 끝(0번 인덱스와 N-1번 인덱스)에서부터 시작해서 안쪽으로 오면서 값이 같은지 비교한다.
- 하나라도 다르면 대칭이 아니다.
- 배열의 절반만 순회하면 된다. (n / 2)

입력 예시 1 :
N 입력 : 5
1번째 정수 : 1
2번째 정수 : 2
3번째 정수 : 3
4번째 정수 : 2
5번째 정수 : 1

출력 예시 1 : 대칭 배열입니다.

입력 예시 2 :
N 입력 : 4
1번째 정수 : 10
2번째 정수 : 20
3번째 정수 : 20
4번째 정수 : 30

출력 예시 2 : 대칭 배열이 아닙니다.
```

## 코드 블록 26

```text
import java.util.Scanner;

public class ArrayPalindrome {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        boolean isPalindrome = true;
        for (int i = 0; i < n / 2; i++) {
            if (nums[i] != nums[n - 1 - i]) {
                isPalindrome = false;
                break;
            }
        }
        if (isPalindrome) {
            System.out.println("대칭 배열입니다.");
        } else {
            System.out.println("대칭 배열이 아닙니다.");
        }
        sc.close();
    }
}
```

## 코드 블록 27

```text
학생 N명의 국어, 영어, 수학 점수를 입력받아 2차원 배열에 저장하고, 각 학생의 평균과 합격 여부를 출력하는 프로그램을 작성하시오.

조건1. 평균 60점 이상이면 합격, 미만이면 불합격이다.
조건2. 평균은 소수점 첫째 자리까지 출력한다.

입력 예시 :
학생 수 : 3
1번 학생 (국어 영어 수학) : 80 70 90
2번 학생 (국어 영어 수학) : 50 40 55
3번 학생 (국어 영어 수학) : 70 65 80

출력 예시 :
1번 학생 : 평균 80.0점 → 합격
2번 학생 : 평균 48.3점 → 불합격
3번 학생 : 평균 71.7점 → 합격
```

## 코드 블록 28

```text
import java.util.Scanner;

public class PassOrFail {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("학생 수 : ");
        int n = sc.nextInt();
        int[][] scores = new int[n][3];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번 학생 (국어 영어 수학) : ");
            for (int j = 0; j < 3; j++) {
                scores[i][j] = sc.nextInt();
            }
        }
        for (int i = 0; i < n; i++) {
            int sum = 0;
            for (int j = 0; j < 3; j++) {
                sum += scores[i][j];
            }
            double avg = (double) sum / 3;
            String result = avg >= 60 ? "합격" : "불합격";
            System.out.printf((i + 1) + "번 학생 : 평균 %.1f점 → %s%n", avg, result);
        }
        sc.close();
    }
}
```

## 코드 블록 29

```text
3행 3열의 정수를 입력받아 2차원 배열에 저장하고, 전체 합계를 출력하는 프로그램을 작성하시오.

입력 예시 :
1행 1열 : 1
1행 2열 : 2
1행 3열 : 3
...
3행 1열 : 7
3행 2열 : 8
3행 3열 : 9

출력 예시 :
전체 합계 : 45
```

## 코드 블록 30

```text
import java.util.Scanner;

public class Array2DSum {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int[][] arr = new int[3][3];
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr[i].length; j++) {
                System.out.print((i + 1) + "행 " + (j + 1) + "열 : ");
                arr[i][j] = sc.nextInt();
            }
        }
        int sum = 0;
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr[i].length; j++) {
                sum += arr[i][j];
            }
        }
        System.out.println("전체 합계 : " + sum);
        sc.close();
    }
}
```

## 코드 블록 31

```text
N행 M열의 정수를 입력받아 2차원 배열에 저장하고, 각 행의 합계를 출력하는 프로그램을 작성하시오.

입력 예시 :
행(N) : 3
열(M) : 3
1행 1열 : 1
1행 2열 : 2
1행 3열 : 3
...
3행 1열 : 7
3행 2열 : 8
3행 3열 : 9

출력 예시 :
1행 합계 : 6
2행 합계 : 15
3행 합계 : 24
```

## 코드 블록 32

```text
import java.util.Scanner;

public class Array2DRowSum {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("행(N) : ");
        int n = sc.nextInt();
        System.out.print("열(M) : ");
        int m = sc.nextInt();
        int[][] arr = new int[n][m];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                System.out.print((i + 1) + "행 " + (j + 1) + "열 : ");
                arr[i][j] = sc.nextInt();
            }
        }
        for (int i = 0; i < n; i++) {
            int sum = 0;
            for (int j = 0; j < m; j++) {
                sum += arr[i][j];
            }
            System.out.println((i + 1) + "행 합계 : " + sum);
        }
        sc.close();
    }
}
```

## 코드 블록 33

```text
N행 M열의 정수를 입력받아 2차원 배열에 저장하고, 각 열의 합계를 출력하는 프로그램을 작성하시오.

입력 예시 :
행(N) : 3
열(M) : 3
1행 1열 : 1
1행 2열 : 2
1행 3열 : 3
...
3행 1열 : 7
3행 2열 : 8
3행 3열 : 9

출력 예시 :
1열 합계 : 12
2열 합계 : 15
3열 합계 : 18
```

## 코드 블록 34

```text
import java.util.Scanner;

public class Array2DColSum {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("행(N) : ");
        int n = sc.nextInt();
        System.out.print("열(M) : ");
        int m = sc.nextInt();
        int[][] arr = new int[n][m];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                System.out.print((i + 1) + "행 " + (j + 1) + "열 : ");
                arr[i][j] = sc.nextInt();
            }
        }
        for (int j = 0; j < m; j++) {
            int sum = 0;
            for (int i = 0; i < n; i++) {
                sum += arr[i][j];
            }
            System.out.println((j + 1) + "열 합계 : " + sum);
        }
        sc.close();
    }
}
```

## 코드 블록 35

```text
N개의 OX 퀴즈 정답과 학생 답안을 입력받아 점수와 결과를 출력하는 프로그램을 작성하시오.

조건1. 정답과 학생 답안은 1(O) 또는 0(X)으로 입력한다.
조건2. 맞힌 문제는 O, 틀린 문제는 X로 출력한다.
조건3. 연속으로 맞힌 문제는 가산점이 붙는다. (연속 2개 : +1점, 연속 3개 : +2점, ...)
조건4. 가산점 포함 최종 점수를 출력한다.

입력 예시 :
문제 수 : 5
정답 입력 : 1 0 1 1 1
답안 입력 : 1 0 1 0 1

출력 예시 :
1번 : O
2번 : O
3번 : O
4번 : X
5번 : O
기본 점수 : 4점
가산점    : 2점
최종 점수 : 6점
```

## 코드 블록 36

```text
import java.util.Scanner;

public class OXQuiz {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("문제 수 : ");
        int n = sc.nextInt();
        int[] answer = new int[n];
        int[] student = new int[n];
        System.out.print("정답 입력 : ");
        for (int i = 0; i < n; i++) {
            answer[i] = sc.nextInt();
        }
        System.out.print("답안 입력 : ");
        for (int i = 0; i < n; i++) {
            student[i] = sc.nextInt();
        }
        int base = 0;
        int bonus = 0;
        int streak = 0;
        for (int i = 0; i < n; i++) {
            if (answer[i] == student[i]) {
                System.out.println((i + 1) + "번 : O");
                base++;
                streak++;
                if (streak >= 2) {
                    bonus++;
                }
            } else {
                System.out.println((i + 1) + "번 : X");
                streak = 0;
            }
        }
        System.out.println("기본 점수 : " + base + "점");
        System.out.println("가산점     : " + bonus + "점");
        System.out.println("최종 점수 : " + (base + bonus) + "점");
        sc.close();
    }
}
```

## 코드 블록 37

```text
0부터 9 사이의 정수 N개를 입력받아 배열에 저장하고, 각 숫자가 몇 번 등장했는지 출력하는 프로그램을 작성하시오.

조건1. 등장 횟수가 0인 숫자는 출력하지 않는다.
조건2. 가장 많이 등장한 숫자(최빈값)를 출력한다. 동률이면 모두 출력한다.

입력 예시 :
N 입력 : 11
1번째 정수 (0~9) : 3
2번째 정수 (0~9) : 1
3번째 정수 (0~9) : 4
...
11번째 정수 (0~9) : 5

출력 예시 :
1 : 2번
2 : 1번
3 : 2번
4 : 1번
5 : 3번
6 : 1번
9 : 1번
최빈값 : 5
```

## 코드 블록 38

```text
import java.util.Scanner;

public class Frequency {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 (0~9) : ");
            nums[i] = sc.nextInt();
        }
        int[] count = new int[10];
        for (int i = 0; i < n; i++) {
            count[nums[i]]++;
        }
        int max = 0;
        for (int i = 0; i < count.length; i++) {
            if (count[i] > max) {
                max = count[i];
            }
        }
        for (int i = 0; i < count.length; i++) {
            if (count[i] > 0) {
                System.out.println(i + " : " + count[i] + "번");
            }
        }
        System.out.print("최빈값 : ");
        for (int i = 0; i < count.length; i++) {
            if (count[i] == max) {
                System.out.print(i + " ");
            }
        }
        System.out.println();
        sc.close();
    }
}
```

## 코드 블록 39

```text
N x N 크기의 정수를 입력받아 2차원 배열에 저장하고, 주 대각선과 반 대각선의 합계를 출력하는 프로그램을 작성하시오.

조건1. 주 대각선 : 왼쪽 위 → 오른쪽 아래 (i == j)
조건2. 반 대각선 : 오른쪽 위 → 왼쪽 아래 (i + j == n - 1)

입력 예시 :
N 입력 : 3
1행 1열 : 1
1행 2열 : 2
1행 3열 : 3
...
3행 1열 : 7
3행 2열 : 8
3행 3열 : 9

출력 예시 :
주 대각선 합 : 15  (1 + 5 + 9)
반 대각선 합 : 15  (3 + 5 + 7)
```

## 코드 블록 40

```text
import java.util.Scanner;

public class Array2DDiagonal {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[][] arr = new int[n][n];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                System.out.print((i + 1) + "행 " + (j + 1) + "열 : ");
                arr[i][j] = sc.nextInt();
            }
        }
        int mainDiag = 0;
        int antiDiag = 0;
        for (int i = 0; i < n; i++) {
            mainDiag += arr[i][i];
            antiDiag += arr[i][n - 1 - i];
        }
        System.out.println("주 대각선 합 : " + mainDiag);
        System.out.println("반 대각선 합 : " + antiDiag);
        sc.close();
    }
}
```

## 코드 블록 41

```text
N x N 크기의 정수를 입력받아 2차원 배열에 저장하고, 전치 행렬을 출력하는 프로그램을 작성하시오.

조건1. 전치 행렬 : arr[i][j]와 arr[j][i]를 서로 바꾼다.

힌트 :
- 새 배열을 만들지 않고 원본 배열에서 직접 swap한다.
- i행 j열과 j행 i열을 바꾸는데, 대각선 기준 한쪽만 순회하면 된다. (j = i + 1부터 시작)
- 대각선 요소(i == j)는 바꿀 필요가 없다.

입력 예시 :
N 입력 : 3
1행 1열 : 1
1행 2열 : 2
1행 3열 : 3
...
3행 1열 : 7
3행 2열 : 8
3행 3열 : 9

출력 예시 :
전치 행렬 :
1 4 7
2 5 8
3 6 9
```

## 코드 블록 42

```text
import java.util.Scanner;

public class Array2DTranspose {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[][] arr = new int[n][n];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                System.out.print((i + 1) + "행 " + (j + 1) + "열 : ");
                arr[i][j] = sc.nextInt();
            }
        }
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                int temp = arr[i][j];
                arr[i][j] = arr[j][i];
                arr[j][i] = temp;
            }
        }
        System.out.println("전치 행렬 :");
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                System.out.print(arr[i][j] + " ");
            }
            System.out.println();
        }
        sc.close();
    }
}
```

## 코드 블록 43

```text
정수 N개를 입력받아 배열에 저장하고, 버블 정렬로 오름차순 정렬하여 출력하는 프로그램을 작성하시오.

조건1. Arrays.sort()를 사용하지 않는다.

정렬 방법 :
- 이중 반복문을 사용한다.
- 바깥 반복문(i)은 0부터 1씩 증가하며 패스(pass)를 진행한다.
- 안쪽 반복문(j)은 인접한 두 원소(arr[j], arr[j+1])를 비교하여 앞의 원소가 더 크면 자리를 바꾼다.
- 한 번의 패스가 끝나면 가장 큰 값이 맨 뒤로 이동하므로, 다음 패스에서는 비교 범위가 1씩 줄어든다.

입력 예시 :
N 입력 : 6
1번째 정수 : 5
2번째 정수 : 3
3번째 정수 : 8
4번째 정수 : 1
5번째 정수 : 4
6번째 정수 : 2

출력 예시 : 1 2 3 4 5 8
```

## 코드 블록 44

```text
import java.util.Scanner;

public class BubbleSort {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        for (int i = 0; i < nums.length - 1; i++) {
            for (int j = 0; j < nums.length - 1 - i; j++) {
                if (nums[j] > nums[j + 1]) {
                    int temp = nums[j];
                    nums[j] = nums[j + 1];
                    nums[j + 1] = temp;
                }
            }
        }
        System.out.print("정렬 결과 : ");
        for (int i = 0; i < nums.length; i++) {
            System.out.print(nums[i] + " ");
        }
        System.out.println();
        sc.close();
    }
}
```

## 코드 블록 45

```text
정수 N개를 입력받아 배열에 저장하고, 선택 정렬로 오름차순 정렬하여 출력하는 프로그램을 작성하시오.

조건1. Arrays.sort()를 사용하지 않는다.

정렬 방법 :
- 이중 반복문을 사용한다.
- 바깥 반복문(i)은 0부터 1씩 증가하며 패스(pass)를 진행한다.
- 안쪽 반복문(j)은 i+1부터 끝까지 순회하며 최솟값의 인덱스를 찾는다.
- 패스가 끝나면 찾은 최솟값과 i번째 원소를 한 번만 swap한다.
- 한 번의 패스가 끝나면 가장 작은 값이 맨 앞으로 확정되므로, 다음 패스에서는 i+1부터 시작한다.

입력 예시 :
N 입력 : 6
1번째 정수 : 5
2번째 정수 : 3
3번째 정수 : 8
4번째 정수 : 1
5번째 정수 : 4
6번째 정수 : 2

출력 예시 : 1 2 3 4 5 8
```

## 코드 블록 46

```text
import java.util.Scanner;

public class SelectionSort {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("N 입력 : ");
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print((i + 1) + "번째 정수 : ");
            nums[i] = sc.nextInt();
        }
        for (int i = 0; i < nums.length - 1; i++) {
            int minIdx = i;
            for (int j = i + 1; j < nums.length; j++) {
                if (nums[j] < nums[minIdx]) {
                    minIdx = j;
                }
            }
            int temp = nums[i];
            nums[i] = nums[minIdx];
            nums[minIdx] = temp;
        }
        System.out.print("정렬 결과 : ");
        for (int i = 0; i < nums.length; i++) {
            System.out.print(nums[i] + " ");
        }
        System.out.println();
        sc.close();
    }
}
```
